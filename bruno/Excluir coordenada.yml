info:
  name: Excluir coordenada
  type: http
  seq: 3

http:
  method: DELETE
  url: http://localhost:8080/geolocation/:index
  params:
    - name: index
      value: "10"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
