info:
  name: Alterar parcialmente
  type: http
  seq: 7

http:
  method: PATCH
  url: http://localhost:8080/geolocation/:index
  params:
    - name: index
      value: "0"
      type: path
  body:
    type: json
    data: |-
      {
          "longitude": -31.234
        }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
