info:
  name: Buscar coordenada por index
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:8080/geolocation/:index
  params:
    - name: index
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
